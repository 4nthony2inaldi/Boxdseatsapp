-- Venue photography uses the original venues.photo_url column (present in
-- the base schema, never populated). No DDL required; this file exists to
-- record the decision. Populate via: node scripts/data/seed-venue-images.mjs
-- Event photos remain user-generated by design.
